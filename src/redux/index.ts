import {
  setEmail,
  setPhoneNumber,
  setToken,
  setUser,
  setUsername,
  selectEmail,
  selectPhoneNumber,
  selectToken,
  selectUser,
  selectUsername,
  selectInitialRoute,
} from "./slices/userSlice";

export {
  setEmail,
  setPhoneNumber,
  setToken,
  setUser,
  setUsername,
  selectEmail,
  selectPhoneNumber,
  selectToken,
  selectUser,
  selectUsername,
  selectInitialRoute,
};
